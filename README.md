# UDPTDLTM_final_project

# How to run the docker ?
- Step 01: docker-compose up airflow-init
- Step 02: docker-compose up -d

# How to see the Airflow UI?
In your we browser, open `localhost:8080`
# How to turn off the docker?
docker-compose down
